[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "jupyter-hpc"
authors = [{name = "Bastien Hervé", email = "bastienherve61@gmail.com"}]
dynamic = ["version", "description"]
